# PlatformIO
.pio/
.pioenvs/
.piolibdeps/
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch/

# Build artefacts
*.o
*.elf
*.bin
*.hex

# Secrets — WiFi credentials and any local overrides must never be committed.
# Credentials live only in NVS at runtime (see docs/WIFI.md).
secrets.h
secrets.ini
credentials.h
wifi_credentials.h
*.local.ini

# OS / editor
.DS_Store
*.swp
