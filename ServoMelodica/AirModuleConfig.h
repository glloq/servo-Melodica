#ifndef DRIVERS_AIRMODULECONFIG_H
#define DRIVERS_AIRMODULECONFIG_H

// AirModuleWiring now lives in the persisted core config (Config.h) so pins are
// editable from the web UI / serial console. This header is kept as a thin
// compatibility include.
#include "Config.h"

#endif  // DRIVERS_AIRMODULECONFIG_H
